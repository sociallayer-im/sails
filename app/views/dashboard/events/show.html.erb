<p style="color: green"><%= notice %></p>

<%= render @event %>

<div>
  <%= link_to "Edit this event", edit_dashboard_event_path(@event) %> |
  <%= link_to "Back to events", dashboard_events_path %>

  <%= button_to "Destroy this event", dashboard_event_path(@event), method: :delete %>
</div>
